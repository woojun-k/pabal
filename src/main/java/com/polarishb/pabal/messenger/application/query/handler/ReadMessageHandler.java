package com.polarishb.pabal.messenger.application.query.handler;

import com.polarishb.pabal.messenger.application.query.input.MessageDto;
import com.polarishb.pabal.messenger.application.query.input.ReadMessageQuery;

public class ReadMessageHandler {
    public MessageDto handle(ReadMessageQuery query) {
        // TODO: Implement logic to read a specific message
        // 1. Validate query data (e.g., message exists, user has access to the chat room)
        // 2. Load the Message entity
        // 3. Map the Message entity to a MessageDto
        // 4. Return the MessageDto
        return null; // Placeholder
    }
}
